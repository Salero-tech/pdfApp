# dev
## browser
```bash
npm run dev -- --host
```
## capcacitor
```bash
npm build && npx cap run
```

### adb Android
(first on host machien the in container)
```bash
adb devices
adb tcpip 5555
adb connect <ip>

```